// import { render, screen } from '@testing-library/react';
// import {SDK-list} from './SDK-list';

// test('renders list of installed SDKs', () => {
//   render(<SDK-list />);
//   const linkElement = screen.getByText(/installed/i);
//   expect(arrayElement).toBeInTheDocument();
// });

// test('renders list of uninstalled SDK’s when uninstalled is clicked', () => {
//   render(<SDK-list />);
//   const linkElement = screen.getByText(/uninstalled/i);
//   expect(arrayElement).toBeInTheDocument();
// });